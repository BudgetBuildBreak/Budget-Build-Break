# GitHub copy — paste these

GitHub is not YouTube. Short field, no trailer music. Lead with the rule.

## Profile bio (160 characters max)

```
SoHo lab. Budget the build. Secure the network. Then we break it.
BOMs, tickets, and threat notes behind the videos.
```

Tighter cut:

```
Budget. Build. Break. — SoHo networks on a real budget. Then we break the fabric.
```

## Repository description (the gray line under the repo name)

```
SoHo lab notes for Budget. Build. Break. — BOMs, tickets, fabric maps, and threat one-pagers behind the videos.
```

## Topics / tags to add on the repo

`homelab` `soho` `soc` `wazuh` `mdm` `networking` `network-segmentation` `threat-modeling` `self-hosted` `budget` `youtube`

## Pinned repo blurb

```
The packet behind the camera. Each video is one hop in a SoHo fabric. The BOM, the ticket, and the break live here.
```

Hide or archive `Hello-World`. Pin this repo.
