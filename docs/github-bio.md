# GitHub copy — paste these

GitHub is not YouTube. Short field, no trailer music. Lead with the rule.

## Profile bio (160 characters max)

```
SoHo lab. Budget the build. Secure the network. Then we break it.
BOMs, maps, and threat notes behind the videos.
```

Character count: 118. Room for a city or `@youtube` handle if you want it.

Tighter cut (90):

```
Budget. Build. Break. — SoHo networks on a real budget. Then we break the fabric.
```

## Profile "Achievements" line / status

```
Building the lab in public. Breaking it on purpose.
```

## Repository description (the gray line under the repo name)

```
SoHo lab notes for Budget. Build. Break. — BOMs, fabric maps, and threat one-pagers behind the videos.
```

Shorter:

```
Budget the build. Secure the network. Then we break it. Channel notes + templates.
```

## Topics / tags to add on the repo

`homelab` `soho` `networking` `self-hosted` `threat-modeling` `budget` `youtube`

## Pinned repo blurb (if you pin it)

```
The paper behind the camera. Each video is one hop in a SoHo fabric. The map, the BOM, and the break live here.
```
